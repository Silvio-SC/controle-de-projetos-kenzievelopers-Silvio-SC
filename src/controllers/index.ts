import { createDev, readDev, updateDev, deleteDev, addDevInfo } from "./developers.controllers"
import { createProject, readProject, updateProject } from "./projects.controllers";


export default { 
    createDev, readDev, updateDev, deleteDev, addDevInfo, 
    createProject, readProject, updateProject 
};